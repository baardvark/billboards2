<h1> Add New Billboard </h1>

<h1><%= @chart.id ? 'Edit Billboard' : 'Create Billboard' %></h1>
<%= form_for @chart do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  
  <%= f.submit "New Billboard" %>

<% end %> 